name: Deploy

on:
  push:
    branches: [master]

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger EasyPanel deploy
        run: |
          curl -s "http://2.24.76.187:3000/api/deploy/79dcab317630073752efdcfe9f658e9e2f9ee7dad825eaf1"
